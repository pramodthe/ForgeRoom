{
  "name": "@forgeroom/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./test-harness": "./src/test-harness.ts"
  },
  "scripts": {
    "migrate": "tsx src/migrate-cli.ts",
    "migrate:down": "tsx src/migrate-cli.ts --down",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@forgeroom/contracts": "workspace:*",
    "drizzle-orm": "^0.44.5",
    "postgres": "^3.4.7"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "^4.20.5",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
