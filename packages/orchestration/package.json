{
  "name": "@forgeroom/orchestration",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./session": "./src/session.ts",
    "./turn-queue": "./src/turn-queue.ts",
    "./event-normalize": "./src/event-normalize.ts",
    "./turn-creation": "./src/turn-creation.ts",
    "./stop-correction": "./src/stop-correction.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@forgeroom/contracts": "workspace:*",
    "@forgeroom/trueforge": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
