{
  "name": "@forgeroom/test-fixtures",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json --noEmit",
    "seed": "tsx src/demo-cli.ts seed",
    "reset": "tsx src/demo-cli.ts reset"
  },
  "dependencies": {
    "@forgeroom/contracts": "workspace:*",
    "@forgeroom/db": "workspace:*",
    "postgres": "^3.4.7"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "^4.20.5",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
