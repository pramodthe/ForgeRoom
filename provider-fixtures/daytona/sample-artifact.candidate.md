# ForgeRoom demo sandbox sample

This file is the **candidate** Daytona artifact body for P0.

- Contents are synthetic/public only (ADR-005).
- Keep under 8 KiB.
- Must be published through TrueForge sandbox-file → application artifact storage before ArtifactCard preview.

```text
demo-rec-001 → open
demo-rec-002 → ready
```

Status: verified 2026-08-26 — Daytona SDK upload/download match (sha256 `1026610d…9c369`, sandbox suffix `4c94`). TrueForge→artifact-storage retain path still blocked on `TRUEFORGE_API_KEY`.
