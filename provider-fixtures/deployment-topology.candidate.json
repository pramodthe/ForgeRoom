{
  "schemaVersion": 1,
  "status": "candidate",
  "ownerTask": "P0-000",
  "verificationOwner": "P0-505",
  "localDevelopment": {
    "api": "apps/api on HOST=0.0.0.0 PORT from env",
    "worker": "embedded when FORGEROOM_EMBED_WORKER=true; optional apps/worker",
    "web": "apps/web Vite/dev origin APP_ORIGIN",
    "postgres": "infra/compose.yaml",
    "artifacts": "ARTIFACT_STORAGE_DIR local directory"
  },
  "localPreflightProbe": {
    "status": "verified",
    "probeDate": "2026-08-26",
    "checks": [
      {
        "name": "DATABASE_URL",
        "result": "pass",
        "note": "PostgreSQL forgeroom reachable; schema migrations not applied (workspaces missing)"
      },
      { "name": "COMPOSIO_API_KEY", "result": "pass" },
      { "name": "COMPOSIO_CONNECTED_ACCOUNT_ID", "result": "pass", "redactedSuffix": "nizY" },
      { "name": "DAYTONA_API_KEY", "result": "pass" },
      { "name": "ARTIFACT_STORAGE_DIR", "result": "pass" },
      { "name": "TRUEFORGE_BASE_URL", "result": "pass", "note": "URL set (len 21)" },
      {
        "name": "TRUEFORGE_API_KEY",
        "result": "blocked",
        "note": "missing — blocks harness session/AgentSpec probes"
      },
      {
        "name": "MODEL_PROVIDER_API_KEY",
        "result": "blocked",
        "note": "missing — blocks coworker model preset probe (OD-005)"
      }
    ]
  },
  "demoDeployment": {
    "status": "candidate",
    "shape": "single_web_service_plus_managed_postgres",
    "processModel": "api_embeds_worker",
    "bind": "0.0.0.0:$PORT",
    "filesystem": "ephemeral; durable artifacts must use object storage or mounted disk once verified",
    "diagramPath": null,
    "note": "Exact host (Render or equivalent) and object-storage adapter remain candidate until preflight probe"
  }
}
